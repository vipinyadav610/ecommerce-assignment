import React from 'react';
import { storiesOf } from '@storybook/react';
import ProductList from './ProductList';
storiesOf('ProductList', module).add('ProductList', () => (
  <div>
    <ProductList />
  </div>
));
